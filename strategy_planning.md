# 月次成果レポート — 戦略設計ドキュメント

最終更新: 2026-09-04

## 1. 背景・ミッション

「右腕AI」プロジェクトの追加モジュールとして、「月次成果レポート（Monthly Performance Report）」のPOCを開始する。既存の社内検討では、本機能は「情緒的価値の提供」から「実践的なROIの可視化」への事業戦略転換を体現する、右腕AI最重要機能として位置づけられている。

本ドキュメントは単なる仕様書ではなく、CPO視点での本質検証・再定義の経緯と、その結果としての設計判断を記録するものである。恒久的な行動原則は [CLAUDE.md](./CLAUDE.md) に集約されているため、実装時はそちらを参照すること。本ファイルは「なぜそうなったか」を保持する。

## 2. 参照ドキュメント一覧（Google Drive）

| ドキュメント | fileId | 位置づけ |
|---|---|---|
| 【重要・検討中】月次成果レポート生成機能 | `16YjzwQQHOwjCIGY8JF7QdTAozR2seWU6AEfEHu-rDqA` | 最新の機能仕様。成果の4階層モデルの出典 |
| 【重要】月次成果レポート作成機能 | `1Daa3kT0dSmc8pTNK0aVBiehjAsiD86UhQIDy38OhjQg` | 初期版仕様。背景・目的の記述が詳しい |
| 【右腕AI】月次経営成果レポート（2025年10月度） | `12ETjbk5TzVc7e36mI7nMTqhRNQ_pblNTzcPS-QJuE_w` | 出力サンプル。トーン検証の一次情報 |
| 『社長のAI坊』シリーズ 全体コンセプト | `1ZMAnejaX-zSBliRGadL1UF8mZq7g4N7t6cpLrc_uoi4` | 姉妹モジュールの思想OS。トーン議論の比較対象 |
| 【重要】右腕AIシステムのAIコンパニオンとしての機能性分析 | `1PPzjY1Z0Kf5Ovp_D6QsG2UT4Zxw1Ia6_yhqqBK-O_QM` | 右腕AI全体のコンパニオン設計思想 |

## 3. 出発点：3つの本質仮説とその批判的検証

プロジェクト開始時、ユーザーから「月次成果レポートの本質」に関する3つの仮説が提示された。CPOとして、参照ドキュメントに照らしてこれを批判的に検証した。この検証が、後述する「プロダクトの魂：3つの哲学」（4章）の出発点になっている。

### 仮説1：思考のモデリング

> AIが数値を読み解く論理的な推論プロセスを見せることで、経営者が高度で理論的な考え方を無意識に学べる

**評価：方向性は妥当だが、既存仕様では未実装に近い。**
既存の「アクションインパクト分析」は「Web改修タスク完了直後に問い合わせ数が増加」のような**相関の提示**に留まっており、AIが「なぜその戦術を選んだか」「他にどんな選択肢を捨てたか」という**推論過程そのもの**は可視化されていない。相関の可視化と思考のモデリングは別物であり、経営者が学ぶべきは結果の相関ではなく判断に至る論理の型である。

### 仮説2：デジタルな客観性

> 人間の忖度や感情的バイアスを排した、デジタルAIならではのフラットで冷徹な意見を述べる

**評価：一部誤りを含む仮説。「対象を選んだ客観性」に修正が必要。**
サンプルレポート自体が「感情と価値観の蓄積」という温かい肯定セクションを持ち、成果の4階層モデルでも④心理的安全性が①〜③の土台と明記されている。「フラットで冷徹」という仮説は、この設計思想と矛盾する。孤独な経営者に数値だけを機械的に突きつけるレポートは、共感機能で築いた信頼関係とトーンが分断され、継続利用を損なうリスクがある。

客観性を発揮すべきは「戦略・数値分析の忖度のなさ」であり、「経営者本人の意思決定や価値観への評価」ではない。後者は誠実に承認する対象である。また、真の客観性は「確信度の誠実な開示」を含む。月次という単位は季節性・外部要因との交絡を避けられず、「対話→行動→成果」を単純な因果として断定すると、それは客観性ではなくAIによる過信・こじつけになる。

### 仮説3：第3の選択肢（C案）の提示

> 経営者が二項対立で迷う際、思考の死角を突く「C案」を提示し、思考の幅を拡張する

**評価：右腕AI全体としては実証済みの強みだが、月次レポートというフォーマットへの適用には設計上の注意が必要。**
「水平思考モード」「批判的思考モード」など、C案的な発想はリアルタイムの対話・共創機能側で既に実証されている。月次レポートは本質的に振り返り（lagging）のドキュメントであるため、レポートの中で唐突に新規のC案を生成するのは、対話・共創機能の役割を侵食する越権行為になり得る。

## 4. たどり着いた本質：プロダクトの魂（3つの哲学）

3章の検証、その後の2回にわたるサンプル生成・ストレステスト（8章）を経て、ユーザーとの壁打ちにより、月次成果レポート／右腕AI全体を貫く最も深い行動原理が3点確定した。これはCLAUDE.mdに最上位の設計思想として刻んである。

### ① Aha!モーメントの構造（動と静のサイクル）

日々のリアルタイム対話（動）でAIがA/B二項対立にC案を提示し、経営者の視界を一瞬で押し広げる。月次レポート（静）はその体験を「構造化された戦略」として解説し直し、記憶に定着させる。

これは仮説3の検証で出した結論「C案はリアルタイム対話側の役割であり、月次レポートが新規に発明するのは越権行為」を、さらに一歩先に進めるものである。従来の発想は「レポートは新しい二項対立を検出してC案を提案する」という**事後発見型**だったが、正しくは「レポートは対話中にすでに提示されたC案を、後から構造化して教える」という**定着支援型**の役割である。データスキーマ上も、二項対立の「検出」ではなく、対話内でC案が実際に提示されたことの「記録」を扱う形に改めている（[report_generation_prompt.md](./report_generation_prompt.md)参照）。

### ② モチベーション担保の鉄則（B→Aコンボ）

悪い数字には「①客観的データによるパニック鎮火（B）→②意図的な決断としての価値づけ（A）」の順で必ずアプローチする。

これは、ストレステスト（8章）で見つかった「悪い月・悪い数字への専用ルールが存在せず、アドリブで対応していた」という課題への正式な解になる。当初サンプルレポートで自然に採用していた書き方（事実の直球提示→原因の分解→意図的判断としての価値づけ）を、明示的な順序ルールとして言語化したもの。

### ③ 1年後の究極のゴール（自立した論理的知性の獲得）

経営者がAIの思考プロセスを完全にモデリングし、自力でC案を生み出せる自立型の論理的知性を獲得することが、右腕AIの最終到達点である。

これは仮説1（思考のモデリング）の検証で指摘した「相関の可視化と思考のモデリングは別物であり、経営者が学ぶべきは判断に至る論理の型である」という論点に、明確な期限（1年後）と到達点（自立）を与えるものである。①のAha!モーメント構造は、この自立を実現するための反復練習の仕組みそのものとして位置づけ直される。

### 月次成果レポートの真の本質（再定義）

> 月次成果レポートとは、過去の数値報告書ではなく、**経営者の意思決定パターンを映す鏡であり、来月の思考をより良くするための「構造化されたフィードバックループ」**である。
>
> 報告する実績の母集団は、右腕AIとの関与の有無にかかわらず**会社全体の成果**とする。その上で、戦略・数値分析においては忖度なく客観的（かつ確信度に誠実）であり、経営者本人の意思決定と価値観に対しては誠実に承認する。悪い数字には必ず「客観的データによるパニック鎮火→意図的な決断としての価値づけ」の順（B→Aコンボ）で向き合う。対話中にすでに提示されたC案があれば、来月への提言の中でその思考の型ごと構造化して解説し、記憶への定着を助ける（動と静のサイクル）。全体を貫くのは、終始一貫した「温かい伴走者」の語り口であり、その先にあるのは経営者自身がC案を生み出せるようになる自立である。

## 5. 成果の4階層モデル（既存仕様からの継承）

右腕AIにおける成果とは、単なる数値向上ではなく「感情の安定（孤独解消）が思考の質を高め、それが行動を変え、最終的に数値（ROI）として結実する」という因果連鎖そのもの。

| 階層 | 内容 | 主な担当機能 |
|---|---|---|
| 第1階層（最終成果） | 財務・事業数値的成果 | 経営ダッシュボード |
| 第2階層（行動成果） | 実行・プロセス変革 | 実行支援機能 |
| 第3階層（思考成果） | 意思決定品質の向上 | 共創機能・対話リード機能 |
| 第4階層（土台） | 心理的安全性と時間創出 | 共感機能 |

月次成果レポートは、この4階層を1本のストーリーとして統合提示する役割を持つ。

## 6. システムの現実が設計に与えた制約

検討の途中で、実行支援機能がまだ開発・実装されていないことが判明した。既存仕様の「思考と行動の軌跡」「アクションインパクト分析」は、対話→タスク→KPIという3データソースの突合を前提にしていたため、この前提が崩れた。

**現状のシステム構成要素：**

| 機能 | 状態 |
|---|---|
| 共創機能（対話から戦略・タスクを策定） | 実装済み |
| 経営ダッシュボード（KPIデータ） | 一部のみ（手入力ベース等） |
| 実行支援機能（タスク管理・実行ステータス追跡） | 未実装（構想段階） |

これをきっかけに、より根本的な論点が浮上した：**レポートで報告する実績の母集団は「右腕AIが関与した成果」に限定すべきか、「会社全体の成果」にすべきか。**

CPOとしての見立て：この2つは対立ではなく役割分担で解決できる。

- **母集団（何を報告するか）は「会社全体の成果」にする。** AIが関与したかどうかで実績を取捨選択すると、①その月の本当に大きな出来事が報告から欠落する、②「AIの手柄」だけを選ぶ恣意性が生まれる、という2つのリスクがある。これは仮説2で確立した「忖度のない客観性」の精神と矛盾する。
- **語り（どう意味づけるか）では、AIの関与度を confidence 付きで誠実に切り分ける。** 関与していない実績は「今月の対話には出てきていません」と正直に書く。何でも自分の手柄にするAIより、手柄でないものは手柄でないと言うAIの方が、既存仕様が掲げる「ROIの証明」に対して長期的に説得力を持つ。

この整理により、実行支援機能の欠如も間接的に軽くなった。厳密な「タスク完了ステータス」がなくても、「共創機能に残る対話の決定事項」と「会社全体のKPI（手入力ベースでも可）」さえあれば、上記の"誠実な語り"は成立するため。

## 7. レポート構成

既存仕様の4セクション構成を土台に、4章の再定義を反映する。

1. **エグゼクティブサマリー** — 今月の総括。温かい伴走者トーンでの導入。
2. **対話と決定の軌跡** — 対話テーマ・決定した戦術の一覧。実行支援機能が存在しないため、実行ステータス列は現時点では持たない（実装後に追加）。
3. **KPIハイライト** — 会社全体のKPIをすべて対象とし、対話との関連付けができるものには確信度付きの相関コメントを、関連付けができないものには「今月の対話には出てきていません」と誠実に明記する。良い数字は因果ルールで、悪い数字はB→Aコンボで書く。
4. **感情と価値観の蓄積** — LTMに基づき、経営者が今月大切にしていた価値観を承認する。
5. **来月への提言（Next Steps）** — 優先課題と最初のアクション。今月すでに対話中で提示されたC案があれば、その思考の型ごと構造化して解説し、Aha!モーメントを記憶に定着させる（動と静のサイクル）。

## 8. プロンプト設計と検証の記録

6・7章の設計を実装可能な形に落とし込んだのが [report_generation_prompt.md](./report_generation_prompt.md)（生成プロンプト完全版）である。これまでに2回のサンプル生成・検証を行っている。

**1例目 [sample_output.md](./sample_output.md)**：製造業・良い月のケースで、トーンの統一・因果表現のconfidence出し分け・C案の自然な統合を検証。「感情と価値観の蓄積」で解釈系語尾が重複する問題が見つかり、「表現の多様性」ルールを追加して対処。その後、実行支援機能の未実装が判明したのに伴い、`tasks`をスキーマから除外し、`kpis`に`linked_dialogue_dates`を追加して非関与ケース（confidence: null）を表現できるよう改訂した。

**2例目 [sample_output_2.md](./sample_output_2.md)（ストレステスト）**：業種を美容室に変え、①悪い数字（売上目標未達）を含む月 ②二項対立が同月に2件 ③価値観が3件、という未検証の条件を重ねて検証した。二項対立の1件選定ロジックと非関与KPIの誠実な記述は機能を確認できたが、以下5点の改善候補が見つかった。

1. 「悪い月・悪い数字」への専用ルールがプロンプトに存在しない
2. 二項対立が複数ある月の選定基準（重要度 vs 直近）が曖昧
3. 選ばれなかった二項対立が跡形もなく消える
4. 「表現の多様性」ルールとconfidence指定フレーズが構造的に衝突しうる
5. `values_ltm`が3件以上のときの型がプロンプトに定義されていない

**完全版への統合**：上記5点に加え、ユーザーとの壁打ちで確定した3つの哲学（4章）を統合し、report_generation_prompt.mdを完全版に改訂した。主な変更は、`binary_trap_detected`／`binary_trap_note`を`c_case`（対話中にリアルタイムで提示されたC案の記録。`reasoning_pattern`＝思考の型を含む）へ置き換えたこと、B→Aコンボの明文化、C案選定基準の明文化と軽いバックログ的言及の追加、表現多様性ルールの例外規定、`values_ltm`の件数別の型の明示、である。

1例目・2例目は旧スキーマ（`binary_trap_detected`）での検証記録として、当時の到達点を示す資料のまま保存してある。

**最終テスト [final_test_output.md](./final_test_output.md)（3ヶ月越しの伏線回収シナリオ）**：完全版に対する初の再検証として、4月の過去決定が7月のKPIに結実するシナリオでテストした。哲学2（B→Aコンボ）は合格したが、哲学1（伏線回収）は正式スキーマが存在せず`past_decision_callback`という仮フィールドでアドリブ対応したこと、哲学3（C案）はテストデータに今月の`c_case`が含まれておらず検証不可能だったこと（ただし「無理に絞り出さない」原則が正しく機能した点は評価）が判明した。

**最終テスト2.0 [final_test_output_2.md](./final_test_output_2.md)**：上記2つの穴を report_generation_prompt.md に正式反映（`past_decisions`／`kpis.linked_past_decisions`の追加、`dialogues`が空の場合のルール明記）した上で、今月の`c_case`を含むデータで再テストした。3つの哲学すべてが、CPOの即興処理を挟まない正式なプロンプトルールとして連動することを確認した。

## 9. POCスコープ（Do / Not Do）

**Do（今回のPOCで扱う）**
- 単月の対話ログ（共創機能）・会社全体のKPIデータから、7章の5セクション構成のレポートを生成するロジックの設計
- 温かい伴走者トーンのライティングガイドライン確立
- 因果分析における確信度の誠実な表現方法の設計（対話と無関係な実績を正直に「関与なし」と書くことを含む）
- 悪い数字の書き方（B→Aコンボ）の設計
- C案統合の判定条件・選定基準の設計

**Do（今回のPOCで扱う・2026-09-04追加、実装済み）**
- レポート履歴機能：過去レポート（入力データ・出力Markdown）の永続化・一覧・再表示。永続化層はNeon Postgres（Vercel連携、`@neondatabase/serverless`使用。`@vercel/postgres`は非推奨のため不採用）。`app/api/reports`・`app/api/reports/[id]`・`lib/db.ts`として実装済み。**ただし実データベース接続（`DATABASE_URL`／`POSTGRES_URL`）は未作成**。ユーザーがVercelダッシュボードでNeon Postgresを作成し接続文字列を設定するまでは、履歴・自立度スコアの永続化だけが無効化された状態でレポート生成本体は動作する
- 自立度スコア（A'）：`reasoning_pattern`を「批判的思考（Critical Thinking）」「水平思考（Lateral Thinking）」の2モードに固定タクソノミー化した上で、月次の「経営者本人主導で型が使われた回数 ÷（本人主導＋AI提示のc_case合計）×100」を算出し、複数月の推移として提示する機能。`lib/autonomyScore.ts`・`lib/systemPrompt.ts`（「5. 来月への提言」内`### 自立度スコア`）として実装済み。ローカルの単発生成テストで、初回月のスコア表示（B→Aコンボ不要ケース）を確認済み。複数月にまたがる停滞・後退時のB→Aコンボ適用は、実DB接続後の複数月連続生成テストで要確認

**Not Do（今回は対象外）**
- 複数月をまたぐ有言実行トラッキング機能
- PDF出力・Web閲覧などの配信フォーマット実装
- 実行支援機能ベースのタスク完了ステータス管理（実行支援機能の実装後に追加検討する。それまでは「対話と決定の軌跡」に実行ステータス列を持たない）

## 10. 意思決定ログ

議論の詳細な経緯は各章に記載の通り。ここでは意思決定そのものを時系列で一覧化する。

| # | 決定事項 | 理由（要約） |
|---|---|---|
| 1 | トーンは終始「温かい伴走者」で統一する（CPO推奨の層別使い分け案は不採用） | ブランド全体との一貫性を優先し、レポートだけ別人格に見えるリスクを避けるため |
| 2 | C案は独立セクション化せず「来月への提言」に統合する | 月次レポート（振り返り）と対話・共創機能（意思決定支援）の役割の境界を守るため |
| 3 | 有言実行トラッキングは今回のPOCスコープ外 | 単月スナップショットのレポート生成にまず集中するため |
| 4 | レポートの母集団は「右腕AIが関与した成果」ではなく「会社全体の成果」とする | 関与の有無で実績を選ぶと、大きな出来事の欠落や恣意的な手柄選びのリスクがあるため（6章） |
| 5 | C案の役割を「レポートによる新規発見」から「対話で起きたAha!モーメントの構造化・定着」へ転換する | ストレステストで見つかった課題と、ユーザーとの壁打ちで確定した哲学①による（4章） |
| 6 | 悪い数字は必ず「B→Aコンボ」の順序で書く | ストレステストで見つかった「悪い月への専用ルール不在」への正式な解（4章） |
| 7 | 経営者の自立（1年後、自力でC案を生み出せること）を最終ゴールとして全設計原則に反映する | 仮説1の検証で指摘した「学ぶべきは論理の型」という論点に、明確な期限と到達点を与えるため（4章） |
| 8 | `past_decisions`／`kpis.linked_past_decisions`を正式スキーマ化し、数ヶ月越しの伏線回収を扱えるようにする | 最終テストで、哲学①（伏線回収）に対応する正式なフィールドが存在せず、仮フィールドでアドリブ対応していたことが判明したため（8章） |
| 9 | 差別化機能として「自立度スコア（A'）」をPOCスコープに追加する。ただし履歴機能（過去レポートの永続化・一覧・再表示）を先に実装し、その基盤の上にA'を乗せる順序で進める | 第7回研修（2026-09-04）で講師から「生成AIの進化で置き換えられない差別化」を問われたのが発端。A'は複数月のデータ蓄積に依存するため、履歴機能なしには成立しないことがコード確認（`app/api/generate/route.ts`に永続化層が存在しない）で判明したため |
| 10 | 永続化層にVercel Postgres（Neon）を新規導入する | 現行デプロイ環境（Vercel）と親和性が高く、POC規模なら無料枠に収まるため（他候補：既存Supabase等の流用、ローカルファイル保存は比較検討の上で不採用） |
| 11 | `reasoning_pattern`の固定タクソノミーを「批判的思考（Critical Thinking）」「水平思考（Lateral Thinking）」の2モードに確定する | 新規に発明せず、Google Drive参照ドキュメント「月次成果レポート生成機能（最新版）」の第3階層記述に既存定義されていたモード名をそのまま採用したため。CLAUDE.mdの「前提を疑う／制約を再定義する／対立軸をずらす」は水平思考モード内の技法例と位置づける |
| 12 | 自立度スコアv1の算出式を「経営者本人主導で型が使われた回数 ÷（本人主導＋AI提示のc_case合計）×100」とし、該当対話が0件の月はスコアを出さない（null）。比較は本人の過去（縦）のみとし、他経営者との横比較（ベンチマーク／集合知案）は採用しない | 「無理に絞り出さない」既存原則を踏襲するため。また横比較案（候補H・I：匿名集合知ベンチマーク、型の的中率学習）はユーザー判断で不採用としたため |
| 13 | 自立度スコアが停滞・後退した月は、既存のB→Aコンボ原則を厳格適用する。この適用範囲をCLAUDE.mdの設計原則に明文化する | 数値化した指標が下がった月に、経営者への評価点に見えてしまうリスクを防ぐため（原則①「温かい伴走者」との整合） |
| 14 | 「運営側は経営者個別の対話ログを認識・アクセスしない」という建前を、CLAUDE.mdの設計原則⑧として明文化する | 2026-09-05の壁打ちでユーザーから提示された前提。月次レポート生成が経営者本人のセルフサービス的なプッシュ操作のみで行われ、運営側による自動トリガー・監視・管理者による編集/削除権限が存在しない設計だったのは、この建前を守るためだったと判明した。候補H・I（決定ログ#12参照）を不採用にした判断も、この建前との矛盾が理由の一つだったことが後付けで整合した |
| 15 | 履歴機能（月次レポート生成アプリ）は単一会社専用とする。会社名選択UIは廃止する | 実際の右腕AIは1顧客＝1インスタンスの想定であり、複数社を切り替えるマルチテナントUIは実態に合わないため |
| 16 | 月次レポートは1ヶ月1件、確定後は再生成不可とする。DBレベル（一意制約）でも重複登録を物理的に不可能にする | 「確定＝二度と作り直せない」を運用ルールだけでなくシステムとして保証するため。UIの誤操作・API直接アクセス・将来の仕様変更による重複registrationのリスクを排除する |
| 17 | 確定済みレポートは内容を一切書き換えない。参照元データ（KPI実績・対話ログ）が事後に訂正された場合も、訂正は翌月以降のレポート内で言及するにとどめる | 上場企業の決算訂正（修正再表示）と同様の発想。レポート層での事後修正を認めると「成果の偽造」につながるとユーザーが明言。そもそもKPI・対話ログはユーザーの手入力ではなく右腕AIのデータベースから拾い上げる値である前提のため、レポート層での「訂正」という概念自体が本来存在しない |
| 18 | トップ画面を「月ボタン一覧」（利用開始月〜前月まで常に全表示、未来月は表示しない）とする。既存の「履歴」フルスクリーン画面は廃止し、月ボタン一覧に統合する | 年月を都度選択する自由入力UIより、確定状態が一覧で一目でわかる方が「1ヶ月1件・確定後不可」という制約と自然に整合するため |
| 19 | 月の確定は厳密に古い月からの順番を強制する。未確定の月が1つでもあれば、それより後の月は生成できない | 自立度スコアの「前月スコア」の取得が常に連続した直前月を指すことを保証し、レポートの物語としての連続性（伏線回収等）も守るため |
| 20 | 生成可能な最新月は常に「前月」に固定する。進行中の今月は生成対象に含めない | 月次実績はその月が終わってから確定する数字であり、進行中の月を「確定」してしまうと実態と矛盾するため |
| 21 | 入力画面の年月・会社名プルダウンは廃止し、遷移元の月ボタンで確定した年月を表示専用で示す | 月・会社の選択はすでにトップ画面（月ボタン一覧）で確定しているため、入力画面で再度選べる状態は二重管理・選択ミスの温床になるため |
| 22 | 生成（＝確定）ボタンを押す前に「この月は二度と作り直せません。よろしいですか？」という確認ダイアログを表示する | 不可逆な操作の前に一拍置く、一般的な安全設計の定石。誤タップでの確定を防ぐため |
| 23 | レポートの表示をポップアップに統一する（生成直後はストリーミング進捗チェックリスト→完成本文へと同じポップアップ内で切り替わり、過去レポートの確認も同じUIで行う）。既存の「結果」フルスクリーン画面は廃止する | 生成直後と過去確認で表示体験を分ける必要が無く、画面・コンポーネントを1つに集約した方が保守コストも下がるため |
| 24 | 小松先生の宿題6点のうち「生成結果の編集機能（保存前に画面内で修正できるようにする）」は実装しない | 決定ログ#17（確定済みレポートは不変・訂正不可）と正面から矛盾するため。生成結果を経営者が書き換えられる余地を作ること自体が、成果の偽造につながるとユーザーが明言（2026-09-05） |
| 25 | 「内容を書き換える」機能とは別に、「生成結果を見てから確定を止める」レビュー工程を設ける。生成完了後、DB保存前に「この内容で確定する／やり直す」の最終確認を挟む | 決定#24で編集機能を不採用にした一方、AI自身の生成ミス（数値誤記・文章破綻・B→Aコンボ順序違反等、入力データの誤りではなくAIの出力層の不具合）に人間が気づいても止める手段が無い、という穴が実装確認で判明したため。「内容を書き換える」ことと「生成物を見て却下する」ことは別物と整理した |
| 26 | 決定#22の「二度と作り直せません」という警告文言は、生成前ではなく決定#25の「確定する」ボタン側に移す | 「やり直す」が可能になった以上、生成前の時点ではまだ何も確定しておらず不可逆ではない。真に不可逆なのは「確定する」を押した瞬間のみであるため |
| 27 | 「やり直す」（AI再生成）に回数制限は設けない。ただし「やり直す」を押すたびにAPI料金が発生することを軌道上に小さく表示する | 回数制限は運用を硬直化させるだけで、乱用防止としての実効性も薄い。一方でコストへの自覚は常に見える場所に置くべきという判断（小松先生の元々の指摘「空欄のまま生成するとAPI料金が1回分かかる」と同種の配慮） |
| 28 | 生成完了後、「確定する」「やり直す」のどちらも押さずにポップアップを閉じる・離脱した場合、その生成結果は一切保存されず消える | DBに書き込まないのが最も安全側に倒れた挙動であるため。次にその月を開けば「未生成」からやり直すだけで済み、実装もシンプルになる |
| 29 | 入力画面（「②本来は右腕AIの過去ログデータベースから自動連携される情報」のシミュレート入力フォーム）を廃止する。「未生成」ボタンを押した瞬間に生成が始まり、ポップアップが開く | 出力（AIの生成結果）の編集を決定#24で禁止しても、入力側（KPI・対話内容）が自由編集可能なフォームとして残っていれば、経営者が都合の良い数字を打ち込むことで実質的に同じ偽造行為ができてしまい、決定#24が骨抜きになるため。会社名・年月の選択も既に廃止済み（決定#15, #21）で、入力画面に実質的な入力項目が残っていなかったことも後押しした |
| 30 | POCにおける各月のシミュレートデータ（`past_decisions`/`dialogues`/`kpis`/`values_ltm`）は、画面上のフォームではなくコード内に月ごとの固定データとして保持し、経営者からは一切見えない・触れない形にする | 決定#29の直接の帰結。実際の右腕AIでは経営ダッシュボード等から自動連携される想定であり、POCでもその「経営者からは触れない」という不可視性そのものを再現する必要があるため |
| 31 | 「確定する」（`saveReport`）はDB未接続時に黙って成功したフリをせず、明示的なエラー（503・`DatabaseUnavailableError`）を返す。履歴一覧・スコア取得等の「読み取り」はDB未接続時に空を返す既存の劣化方針のままでよいが、「確定」という書き込み操作だけは別扱いにする | 実機確認で、DB未接続のまま確定するとAPIが`{"ok":true}`という偽の成功を返し、UIがポップアップを閉じて確定できたように見せてしまうバグが発覚した。確定は不可逆な操作であるため「できたと見せかけて実は保存されていない」は経営者への裏切りに直結し、他の読み取り系の静かな劣化とは性質が異なると判断した |
| 32 | KPIハイライト表に「単位」列を新設し、前月・今月・目標の数値からは単位表記を省く | 小松先生のUI/UX指摘（表の体裁：単位が数値セルに繰り返し出て読みにくい）への対応。指標ごとに単位が異なる（円・%・件）ため列見出し1つには集約できず、単位専用の列を追加する形にした |
| 33 | 自立度スコアの数字（点数のみ、比較・評価なし）を「1. エグゼクティブサマリー」にも露出する。比較・B→Aコンボの展開は引き続き「5. 来月への提言」に集約する | ユーザーから、差別化機能である自立度スコアを冒頭でも見せたいとの要望。当初提案の「サマリーに全文言及」は却下し（B→Aコンボの余白が無くなる・サマリーの総括という役割と衝突するため）、「数字のみ・評価なし」という折衷案で合意した |
| 34 | 決定#33を撤回し、エグゼクティブサマリーから自立度スコアへの言及を完全に除く。代わりに「2. 自立度スコア」という独立セクションを新設する（KPIハイライトと同格の一級セクション扱い。スコアがnullの月はこのセクションごと省略し、以降の番号は詰めない） | 実際に生成した4月度レポートで「自立度スコアは0点でした」がサマリーに出た結果、評価語が無くても数字自体が強い第一印象を与えることが判明したため。独立セクション化によりB→Aコンボの展開に十分な余白を確保しつつ、冒頭に近い位置で存在感を保てる |
| 35 | 「5. 来月への提言」を「6. 〔next_report_month〕への提言」に変更し、見出し・中見出しに実際の年月を明記する（「来月」という相対表記は使わない）。次月の年月は`lib/months.ts`の`getNextMonth`でコード側が計算し、`next_report_month`として渡す | 履歴機能により確定済みレポートは何ヶ月後にも読み返される前提になったため、「来月」という執筆時点基準の相対表現は、後から読むと「いつの来月か」曖昧になる。年またぎの計算をLLMに任せるとズレるリスクもあるため、コード側で決定的に計算する |
| 36 | レポート本文・表内の日付は、レポート対象年（`report_year`）と同じ年なら年を省略し「4/8」、異なる年のみ「2025/12/1」のように年を含める。日を含む場合は必ず半角スラッシュ区切りにする（`report_month`／`next_report_month`等の月単位ラベルは対象外） | ユーザーが、ダウンロードした`.md`ファイルを別ビューアで開くと日付が単語の途中（「4月」「8日」の間）で改行される問題を発見。CSSの`word-break: keep-all`（決定#37）はアプリ内表示にしか効かないため、ダウンロード後も再現しない根本対策として表記そのものを変更した。さらにユーザーから、レポートの題名で年はすでに分かっているため日付ごとに年を繰り返すのは冗長との指摘があり、年省略ルールを追加した |
| 37 | `.report`（レポート表示コンテナ）に`word-break: keep-all`＋`overflow-wrap: break-word`を適用する | 「2026年4月8日」「目標達成」等の熟語・日付がアプリ内表示で単語の途中改行される問題への対処（決定#36と対になる。ただしこちらはアプリ内表示のみに効き、ダウンロード後の別ビューアには効かないため、決定#36の表記変更で根本対応を補完する） |
| 38 | Neon Postgres（`DATABASE_URL`／`POSTGRES_URL`）が未設定の場合、`lib/db.ts`は`lib/fileStore.ts`（プロジェクト直下`.data/reports.json`への読み書き）に自動フォールバックする。決定#31の`DatabaseUnavailableError`は削除し、確定操作は常に実際に成功するようにする | PoCデモを外部DBの契約・環境変数設定なしで動かしたいというユーザーの要望。ローカルJSONファイルは本番のVercelサーバーレス環境では永続化されないため、ローカル実行専用の位置づけ（`.gitignore`に`.data/`を追加）。Neon Postgres対応のコードは変更せず残し、`DATABASE_URL`を設定すればそのまま本番用の永続化に切り替わる |
| 39 | 月一覧をタイル状のグリッド表示に変更（年は「2026年」と1度だけ見出しで示し、各タイルは月のみ表示）。ポップアップには会社名（`COMPANY_NAME`）を月度見出しの上に追加表示する。ただしAI生成本文（Markdown）からは会社名を削除しない | ユーザーから月一覧の視認性向上（横幅・縦幅の圧縮）の要望。会社名の重複表示についても本文から削除する案が出たが、Google Drive参照ドキュメントに「社外への説明資料：金融機関やステークホルダー等への説明資料として活用できる」という用途が明記されており、ダウンロード後にアプリの外へ持ち出されるとアプリUIの会社名表示から切り離される。ファイル名だけでは信頼できないため、本文中の会社名表記は残すことにした |
| 40 | `.report`から`overflow-wrap: break-word`を外し、代わりに`overflow-x: auto`を付与する。`.modal-dialog`に`min-width: 0`を追加する | 実機確認で、`overflow-wrap: break-word`が日本語の禁則処理（「、」「。」を行頭に置かない）より優先されてしまい、句読点だけが行頭に来る・表が崩れる不具合が発覚した。また`.modal-dialog`がFlexbox内で`min-width`未指定だったため、幅の広い表に押されて箱自体が広がり中央寄せがずれる問題も判明。両方を修正した |
| 41 | KPIハイライト表の「前月比・目標比」列を「前月比」「目標比」の2列に分割し、各セルは符号付き数字のみ（ラベル文字列を含めない）にする | 実機確認で、1セルに前月比・目標比の両方を書かせようとした結果、AIが「目標比」というラベルをセル内に挿入し、表が3行に折り返って崩れる不具合が発覚した。列を分ければラベルが不要になり、セルの中身も短くなる |
| 42 | `next_report_month`にも日付と同じ「年省略」ルールを適用する（`report_year`と同じ年なら「7月度」、年をまたぐ場合のみ「2027年1月度」）。表示形式まで`lib/months.ts`の`formatMonthLabel`でコード側が決定的に計算し、AIには加工させない | ユーザーから、「6. 2026年7月度への提言」の「2026年」がレポート自身の年と同じで冗長ではという指摘。決定#35で「report_month／next_report_monthは年省略ルールの対象外」としたのは判断が浅く、実際には日付と同じロジックがそのまま適用できると判明したため |
| 43 | 文書冒頭（`## 1.`より前）の表題を、会社名・年月度の2つを空行区切りの別々の段落（見た目上2行）にするルールを明文化する。`｜`等の記号で1行にまとめない | これまで表題部分に明示的なルールが無く、AIが自己流で「株式会社サンプル｜2026年6月度 月次成果レポート」と1行にまとめていた。ユーザーから改行で分ける指示があり明文化した |

## 11. Open Questions / Next Steps

- `past_decisions`をいつまで「今も有効な伏線」として保持し続けるか、鮮度・重要度による除外基準が未設計（8章の最終テスト2.0参照）
- `c_case`（対話中のC案提示）は右腕AIの対話機能側（社長のAI坊の水平思考モード等に相当）で生成・記録される前提だが、その実装自体は本プロジェクトのスコープ外。連携インターフェースの設計は未着手
- `reasoning_pattern`の固定タクソノミー（批判的思考／水平思考の2モード）は確定済み（決定ログ#11）。ただし「経営者本人の発言のみで型が確認できるケース」（AI提示のc_case経由でない自走判定）をどう安定的に判定させるかは、プロンプト側の新規判定ロジックとして未検証
- Vercel Postgres（実際にはNeon Postgres／`@neondatabase/serverless`を採用）のテーブル設計・API設計は確定・実装済み（`lib/db.ts`の`reports`テーブル、決定ログ#9〜13）。残る作業は、ユーザーがVercelダッシュボードでNeon Postgresを作成し`DATABASE_URL`（または`POSTGRES_URL`）を設定すること
- 選ばれなかった`c_case`の軽いバックログ的言及は単月内の対応にとどまる。複数月にまたがる本格的なバックログ管理は、有言実行トラッキングと合わせて将来フェーズの検討事項
- 有言実行トラッキングを将来フェーズでどう実装するか（先月提言→今月実行確認のデータ連携設計）
- 複数月にわたる意思決定パターンのトレンド可視化は、月次レポートの拡張として作るか、別モジュールとして切り出すか
- 因果分析の確信度をどう定量的・視覚的に表現するか（例：確信度バッジ、代替要因の併記フォーマット）
- 実行支援機能の実装後、「対話と決定の軌跡」に実行ステータス列をどう復活させるか（`tasks` スキーマの再設計）
- 経営ダッシュボードが手入力ベースである間、KPIデータの更新頻度・精度をどう運用で担保するか
- `kpis` の `linked_dialogue_dates`（対話との関連付け）は誰が/何が判定するのか（人手か、DeepThink等による自動推定か）
- **自立度スコア（A'）が可視化する「自立ゴール」と、事業継続性（サブスクリプション収益）の緊張関係が未検証（2026-09-04、ユーザーとの壁打ちで浮上）。**
  哲学③「1年後、経営者が自力でC案を生み出せるようになること」は本プロジェクト開始当初からの既存の目標だが、A'はそれを初めて「見える数字」として可視化した。これにより「自立度が上がり続ける＝右腕AIが不要になり解約されるのでは」という懸念が明示的になった。
  現時点でのCPOとしての見立て（未検証の仮説）：(1) スコアは月次でリセットされる一時点の指標であり、経営の複雑さが経営者の成長より速く増える限り自立度が高止まりし続けるとは限らない、(2) このスコアが測るのは右腕AIの提供価値の一部（C案発想の型）に過ぎず、経営ダッシュボード自動化・LTM・共感機能等の価値は代替されない、(3) コーチング業・顧問業全般が抱える古典的な緊張関係でもあり、自立支援はむしろ信頼・継続利用意向を強める可能性がある。
  ただしこれは実装で解決できる論点ではなく、事業戦略として次回研修で小松先生とすり合わせるべき前提そのものである。
- 「利用開始月」（月ボタン一覧の起点）をどこに設定値として持つか（ハードコード定数か、将来的な会社ごとの設定値か）は未実装（決定ログ#18関連）
- 既存の`/api/reports`・`/api/reports/[id]`を、月ボタン一覧（利用開始月〜前月の全月について確定有無を返す）の要件に合わせてどう改修するかは未実装（決定ログ#18〜20関連）
- 自立度スコアの複数月推移をレポート内でどう可視化するか（グラフ化するか、文章内の言及にとどめるか）は依然未設計
